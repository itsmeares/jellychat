---
name: "JellyChat"
guid: "a69744cc-2281-48bf-adef-8e451a16ff71"
version: "0.8.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "JellyChat drawer for Jellyfin SyncPlay sessions."
description: >
  Adds a JellyChat drawer to Jellyfin web clients for SyncPlay sessions using a
  plugin-owned in-memory event stream. It does not use Jellyfin toast chat
  transport, an external WebSocket server, or additional network ports.
category: "General"
owner: "itsmeares"
artifacts:
- "Jellyfin.Plugin.JellyChat.dll"
changelog: >
  Polishes the existing JellyChat drawer, timeline, message actions, native
  trigger states, empty/disabled states, touch layouts, accessibility, and
  reduced-motion behavior while keeping the in-memory event stream and
  self-contained injection model unchanged.
